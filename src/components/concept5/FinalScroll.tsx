"use client";

import { motion } from "framer-motion";

export function FinalScroll() {
  // This component is no longer needed in the new editorial layout.
  // The Footer handles the closing CTA. Return null to keep the import clean.
  return null;
}
